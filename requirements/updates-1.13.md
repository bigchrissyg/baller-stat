# Updates for v1.13
## Edit Match
When editing a match with no players attendance records, the interface should add ALL players by default, so then you can remove the ones you don't use later.

## Debug
The debug window should be moved to the bottom. There should be a toggle to show or hide. It should start as hide by default.